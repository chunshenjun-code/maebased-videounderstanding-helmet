# ============================================================
# 安全帽佩戴检测 - 视频动作分类训练环境依赖
# 安装方式：pip install -r requirements.txt
# PyTorch 请根据 CUDA 版本从 https://pytorch.org 单独安装
# ============================================================

# ---------- 基础核心库 ----------
numpy>=1.21.0
opencv-python>=4.5.0
opencv-contrib-python>=4.5.0
pillow>=9.0.0
tqdm>=4.60.0
matplotlib>=3.4.0

# ---------- PyTorch 生态 (请自行安装 CUDA 版本) ----------
# PyTorch 安装示例 (CUDA 11.8):
#   pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cu118
# PyTorch 安装示例 (CPU only):
#   pip install torch torchvision
# 以下仅用于依赖声明，实际不会自动安装 GPU 版本
torch>=1.13.0
torchvision>=0.14.0

# ---------- 机器学习 ----------
scikit-learn>=1.0.0
joblib>=1.1.0

# ---------- HuggingFace Transformers (VideoMAE / TimeSformer) ----------
transformers>=4.30.0
sentencepiece>=0.1.99      # transformers 依赖
protobuf>=3.20.0           # transformers 依赖

# ---------- 手部检测 (MediaPipe) ----------
mediapipe>=0.10.0

# ---------- 数据增强 ----------
albumentations>=1.3.0

# ---------- YOLO 物体检测 (可选，用于背景模糊保留物体) ----------
# 如需 YOLO 物体检测功能 (blur4.py)，需额外安装：
#   pip install ultralytics
#   或克隆 YOLOv5 仓库并使用 torch.hub 加载：
#   git clone https://github.com/ultralytics/yolov5
#   cd yolov5 && pip install -r requirements.txt
# ultralytics>=8.0.0

# ---------- 可视化 (videomae/visualize/ 下的 t-SNE/UMAP/PCA) ----------
# scikit-learn 已包含 PCA 和 t-SNE
umap-learn>=0.5.0           # UMAP 降维可视化
